# P88 TPU Compile Smoke Artifacts

This directory stores reports from `scripts/run_pallas_tpu_smoke.py`.

The committed local report is expected to be `status=unavailable` when generated
on a CPU-only machine. A real TPU run should overwrite
`pallas_tpu_compile_smoke.json` with the observed TPU environment result.
