distillation:
  stage: 0
  target_bundle: artifacts/teacher_targets/fake_export
  student_config: configs/tiny_cpu.yaml
  max_steps: 2
  losses:
    hidden_mse:
      enabled: true
      weight: 1.0
    logits_kl:
      enabled: false
      weight: 0.0
    attention_or_mixer:
      enabled: false
      weight: 0.0
